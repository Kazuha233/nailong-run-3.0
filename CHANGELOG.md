# 奶龙快跑 3.0 🦖 · 更新日志（CHANGELOG）

> 版本节奏：v3.0 = 全新数据库 + Edge Function 安全架构（2026-08-06）
> 3.0 与线上 v2.x 并行：新库 `flhopneudhhwweclcvmo`，旧版线上（kazuha233.github.io/nailong-run）仍用旧库 `stvofpguczogdaxmtqff`
> 开发目录：`D:\R\Desktop\agent项目\nailong-run-3.0\`

---

---

## v3.3.0（2026-08-13）— 强化加点系统 + 能源结晶收集 + 无尽着火机制 + 依神蹲姿修复 + 反篡改加固

- ⚡ **强化面板**：主菜单新增「强化」入口（教程/角色之间）——左侧当前角色放大版（2.6× 实时动画）、右侧角色属性（血量/能量上限/恢复速度，无尽未解锁显示「未解锁」）+ 加点区；右上角 ✕ / ESC 关闭；「切换角色」展示按钮（仅切展示，游戏角色选择不动，带切换语音）
- 💎 **加点系统**：能量上限 +10%/级、恢复速度 +0.5%/秒/级；成本阶梯（每升 5 级所需结晶 +1）；结晶不足按钮变灰禁用；升级音效 + 角色周围蓝色竖条粒子升腾
- 🔷 **能源结晶收集物**：三模式通用（普通/极限/无尽），每 100~150s 刷新一颗；高度两档（低 40~55 跑过即吃 / 高 95~115 必须跳）；数据版水晶 1.5× + 微微上下浮动；拾取 +1 即时同步云端
- 🔋 **游戏内能量条联动强化**：能量上限按加点生效（100+10×Lv）；40% 阈值线按新上限百分比移动；开局回满
- ☁️ **加点/结晶云端同步**：users 表 enhance JSONB 列（与成就同模式，扩展加 key 不改表）+ sync_enhance RPC + enhance Edge Function（Origin 白名单 + token 归属校验）；登录时跨设备取较大值合并；变更链式防并发上传
- 🏷️ **nlEnhance 签名化**：本地数据存 {v 数据, h 校验和}——控制台直接改数据被识破视为默认值；写库前规范化（负数/非数字清除）
- 📶 **请求优化**：回主菜单不再每局拉取云端（登录时拉取一次即可，每局省 3 次请求）

- 🔥 **火堆改版为着火状态机**：触碰不再直接扣血——触发着火（每 0.2s -1 滴血、身上泪滴火苗 + 受火光晕、每 0.8s 受伤音效循环），连续下蹲 3 次扑灭（间隔 >1.5s 重置）；火堆受击区收窄至底部中间（两侧不判伤）
- 🌧️ **雨天火堆熄灭**：下雨时火焰熄灭只剩焦坑 + 4 团微烟、碰撞豁免；随雨势渐变延迟熄灭/复燃（≈1s）；雨天底部湿焦调色
- 🩸 **受伤机制改版**：删除受伤无敌帧——每次接触立即扣血，同一障碍只扣一次（防重叠秒死），连环撞不同障碍连续扣血；奶鱼护体保留
- ❤️ **无尽血量公式**：总血量 = 100 + 10×成就数（成就越多越肉）；血条按 hpMax 比例；低血（<30 滴）呼吸闪烁；主菜单无尽「？」说明弹窗
- 🔊 **无尽音效三件套**：直升机出场专属音效；统一受伤音效（响度增益重制）；障碍撞击处受伤 + 复活双声道叠加；着火 0.8s 循环
- 🧱 **障碍细节终版**：钢筋 2 形态（金字塔/尖塔）+ 撞击散落形态 + 细长钢筋粒子 14 片；矮墙灰系粒子（26+10）；直升机双源粒子（机身/吊货分别从各自中心发出）；木箱数据版（吊高 12px 与蹲姿净空一致）；火星恢复四向散射 + 相对场景左移
- 🔶 **能源结晶强化图标**：面板水晶图标数据版绘制（放大 2 倍、修复画错画布双层根因）

- 🧎 **依神蹲姿/滑铲回归正确**：按 S/↓/← 默认滑铲，荆棘缠身 / 细雪缠身 / 火焰灼烧时强制「蹲」姿态（SQUAT 角度表）；根因=渲染函数内部自调姿态插值覆盖外部传参——squatForce 全链路显式传参贯通（渲染/碰撞盒/特效 6 处）
- 🔥 **依神着火粒子位置**：根因=渲染中心 ≠ 角色坐标（数据 bbox 中心被放大偏移 52px）+ 基准误用数据原点映射点——修正后火焰贴身体中心（含蹲姿适配收窄）
- 🖱️ **强化按钮样式统一**：CSS 联合选择器补齐（曾漏新按钮导致格式不统一）
- 📊 **血条比例失真修复**：旧硬编码 /100 导致高血量满格——改 hpMax 比例制

- 🛡️ **云端数值权威上限**：结晶 ≤100000、加点等级 ≤999（防刷数值/天文数字）；enhance 函数与 RPC 双端白名单校验（key 白名单 + 数值范围 + token 归属）

---

## v3.2.0（2026-08-12）— 成就系统 16 连扩 + 排行榜双模式 + 奶龙荆棘重做 + 依神细雪修复

- 🏆 **成就系统大扩容（8 → 16）**：新增 ☃️风雪压我千百年（细雪缠身 1600 分）/🛡️固若金汤（一局撞碎 15 障碍）/💨以屁为技（释放技能）/♾️无尽时代（无尽 1314 分）/🤖YMYC（切换依神）/🏃你跑不过我（6666 分）/🎇有头有脸（自定义头像）/🏆万人之上（排行榜第一）；主人自定义顺序；🎗️极限雪峰换图标；🌵荆棘行者细雪判定移出（细雪专属 ☃️）；UI 操作型成就 allowUI 通道（主菜单可解锁）
- ☁️ **成就云端同步扩容**：achieve/equip 函数 + RPC 白名单 8 → 16 key（已部署，跨设备全量拉回）；佩戴系统支持全部 16 成就
- 🏆 **排行榜双模式**：无尽模式分数上云（紫色显示优先于黄色极限分排列）；无尽覆盖极限成绩；25 分/秒 · 上限 999999；score RPC 5 参数版 + 部署
- 💥 **结算撞碎统计**：死亡/通关结算显示「撞碎：🪨 5 · 🚁 1 …」（石块/鳄鱼/直升机/吊物分类，只显示 ≥1）；卡片宽度按文字自适应
- 🌿 **奶龙荆棘重做（学依神手绘风）**：蜿蜒主藤横跨身体 + 细卷须 + 绿弧条 + 随机角度尖刺 + 绿点；确定性伪随机（同层不抖动）；集中身子不碰头；工坊新增奶龙荆棘模板 5 层
- ❄️ **依神细雪缠身修复**：根因=游戏主脚本 IIFE 局部 mode 全局函数访问不到 → 永远画荆棘；thMode 显式传参修复
- 🐟 **奶鱼两档高度**：低档 40~55px 跑过即吃 / 高档 95~115px 必须跳（消除中间模糊区）
- 🚁 **障碍概率重调**：鳄鱼 12%→10%（仅黑夜）；直升机 4%→昼 10%/夜 6%（白天鳄鱼区间 4% 归直升机）；石头 70%
- ⏩ **开场零等待**：登录验证与开场视频并行（页面加载即启动，防重入）；进度条 100% 后秒进主菜单
- 🎬 **开场视频竖屏修复**：overlay 随 #stage rotate(90°)，fitVideo 按局部尺寸（微信/QQ 全屏正立）
- 🏆 **成就通知堆叠**：多成就连发全部可见（新在上、旧下移，每条 2.2s 淡出）
- 🔥 **极限模式「？」说明**：按钮右侧小灰圈凸出，弹窗「极限模式：游戏内不刷新奶鱼，初始奶鱼数=成就数x2」；初始奶鱼 = 成就数 ×2；toast 支持长文案换行 + 时长参数
- 🧪 测试版：删 2 个跳过键；主菜单加「一键全成就」作弊按钮（仅测试版）
- 📄 桌面新增《横竖屏自适应教程.md》（供分享）

## v3.1.9（2026-08-12）— 缠身 5 层完成 + 极限终点演出 + 天气特效 + 手机触控/设置大升级

- 🌿❄️ **荆棘/细雪 5 层体系全部完成（主人手绘）**
- 🌿 **奶龙荆棘 L4/L5 分布更广**：角度 ±1.1→±1.4、垂直跨度加大（L5 ±20）、起点错开（横向±2.5+纵向±3）——不再并排一撮
- 🪧 **极限终点指示牌升级**：新模型（粗柱 18.6×410 + 柱脚白片 + 删旧小片，贴地基准动态化）；「雪山出口」文字修正（先定位再局部翻转，位置主人微调至 105,280）；三角/白图形方向修复（y 翻转系 rotate 取反）；**像障碍物一样从右滚入→滚出屏幕** + 临近终点全场景减速 ×0.3（好看清牌子）
- 🌨️ **暴风雪天气特效**：提示改上方横幅（同雪天格式）；雪花快速左移（叠加场景滚动速度）、暴风雪档 300px/s、雪花 ×1.8 + 补密 35 颗（确定性时间驱动防闪烁）+ **7 条大风线特效**（900~1420px/s 左移）；⚠️ 测试开关：开局直接暴风雪（看效果，测完还原）
- 💥 **撞击粒子分色**：直升机按模式分色（普通原版灰绿系 / 极限黄色系 #ffc800 等）；纸箱掉落物专属棕碎片 14 片 + 4 大残骸（两模式通用）
- 📢 **游戏内播报队列**：天气/缠身提示统一中上方（最新 y58、旧的往上移 26px、触顶/超时消失、同文案去重、上限 4 条）
- 📱 **手机触控升级**：技能按钮 💨（经典按键模式，跳上方可自定义）；分区模式**双击跳键 = 技能**（300ms 判定）；**按钮布局自定义**（设置→经典按键内嵌：拖拽移动 + 单击选中调大小 40~120/透明度 30~100% + 保存 nlBtnLayout，布局模式隐藏主菜单、完成回设置）；角色面板竖屏畸形修复（charPanel 移出 #stage → fixed 视口贴右缘）；toast 竖屏歪 90° 修复（挂 #stage 跟随旋转）
- 🕹️📖 **帮助拆分为设置/教程**：两个独立窗口（主菜单右下角 🕹️ 设置 + 📖 教程，各自打开/关闭/ESC，教程右上角 ✕）；设置面板两列排版（左上下分区 / 右经典按键+内嵌自定义布局，下方完成）
- ⌨️ **电脑键位调整**：跳跃 W/↑；蹲滑铲 **S/A/←/↓**；技能 **空格/→**（帮助面板同步）
- ⏭️ **开场跳过**（测试效率）：开场视频右下角「⏭️ 跳过」+ 无尽开场动画右上角跳过（主人要求加回，此前禁跳过规则撤销）
- 🔋 **开局屁量回满 100%**（reset 重置 fartGauge）；🐟 极限模式无限奶鱼测试开关（撞障碍不扣鱼，测完还原）
- 🗑️ **一键通关测试功能**（普通/极限终点速览）使用后已删除；🚀 测试成绩防污染标志（testWinActive）同步清理
- ☁️ 前序 v3.7.0 反作弊 4.0 全体系保留（IIFE 闭包/runToken/成就签名/token 过期/时间连续性）——本版本在其上继续迭代

---

## v3.1.8（2026-08-12）— 随枫反作弊系统 4.0（安全加固 A 档，外部安全审计 9 漏洞响应）

- 🏷️ **系统命名：随枫反作弊系统 4.0**（主人命名）：1.0=基础鉴权（token 归属 + Origin 白名单）→ 2.0=偏差校验（分数-时长偏差 ≤20% + 超速约束 + 限流）→ 3.0=时间连续性（跨提交墙钟 + 涨幅检测）→ **4.0=全体系**（前端 IIFE 闭包 + runToken + 成就签名 + token 过期 + 五重服务端校验 + 双目录运维）；经验沉淀至桌面「网站网安要点.txt」
- 🙈 **错误码模糊化**（方案 A）：score/equip/achieve 全部失败响应统一 `400 {"error":"invalid_request"}`（含 RPC 反作弊错误，不再泄露 suspicious score/too frequent）——攻击者分不清 token 错/分数错/限流/反作弊；保留 405/403（协议层）+ auth 业务码（注册重名/锁定/踢登录 UX 依赖）；线上实测：伪造 token/畸形分数/连刷全返回同一谜团响应 ✓ 正常玩家 61s 后提交 ok ✓

- 🛡️ **全脚本 IIFE 闭包化**（VULN-01/02 缓解）：整个游戏逻辑包进 IIFE——所有函数/状态不再挂 window/全局词法作用域，**console 无法直调 winGame/submitScore/unlockAchievement、无法篡改 s_core**（实测全 undefined）；零 inline handler 零 window 暴露，包装零回归（页面流程正常）
- 🎟️ **本局会话令牌 runToken**（纵深防御）：startGame 签发（crypto.randomUUID）、g_ov/w_gm 结算吊销；submitScore/unlockAchievement 校验令牌——console 即使绕过闭包也因无令牌被拒
- 🔏 **本地成就签名化**（VULN-02 补充）：nlAch 存 `{v, h}`（SHA-256+盐校验和）——console 直接改 localStorage 篡改成被识破（视为空成就）；旧版裸数据首次读取自动升级不丢成就；单元测试：正常写入验证通过 / 篡改拒绝 ✓
- ⏰ **Token 30 天过期**（VULN-09 缓解）：users 新增 token_ts 列；注册/登录/改密码签发时写时间戳；4 个 Edge Function（auth/score/equip/achieve）6 处 token 校验全部加 `.gte(token_ts, 30天前)` 过期过滤——过期 token → 401 重新登录；**线上实测**：伪造 token 401 ✓、真实 token 改 40 天前 → 401 ✓、恢复 → 通过 ✓
- 🔄 **跨提交时间连续性校验**（VULN-05/06 缓解）：upsert_leaderboard v3 新增 ⑤——本次声称 elapsed ≤ 距上次提交真实墙钟间隔 +30s；**线上实测**：首次提交通过 ✓、61s 后连刷 9999/231s → suspicious score 拒绝 ✓
- 🐛 **RPC bug 修复**：⑤ 校验查询从 limit 1 改 **offset 1**（limit 1 会查到本次刚插入的记录 → dt≈0 误拒所有提交；首版部署即踩中，修正后重部署）
- 🗑️ **删除正式版残留「跳过」按钮**（此前某轮同步带回旧版，主人明确禁止）：HTML/CSS/JS 三处清理，实测 skipExists=false
- 📦 **测试版重建**（主人要求，8/12）：D:\R\Desktop\测试版 全新副本（index.html + assets + dev-tools + manifest + sw.js）；**无尽/依神永久锁**（updateEndlessBtn 恒 remove('unlocked') + 无条件拦截「🔒 研发中……」+ isYishenUnlocked 恒 false + 依神卡描述「研发中……」+ 标题「奶龙快跑测试版」）；⚠️ 打锁坑：拦截替换须整段替换（残留 `}` 会让 initAudio/introEndless 变顶层语句，加载时误触发开场动画——已修）

---

## v3.1.7（2026-08-11）— 测试版分版 + 开场视频 + 数据同步修复 + 手机触控大升级

- 📦 **测试版分版**（D:\R\Desktop\测试版）：独立副本（index.html + assets + dev-tools）；**无尽模式 / 依神永久锁**（按钮恒灰 + 无条件拦截 + isYishenUnlocked 恒 false + 标题「奶龙快跑测试版」+ 锁内容显示「🔒 研发中……」）；正式版不受影响；每次改动用「复制 + 精确重打锁」流程同步
- 🎬 **开场视频 + 加载进度条**：打开游戏先播开场动画（assets/opening.mp4，854×480 7.17s，muted 自动播放，**无跳过按钮必须完整播放**）；**边播边预加载**全部资源（BGM Blob 整首 fetch / 图片 / 音效）；播完 → 真实进度条（loaded/TOTAL）→ 淡出进主菜单；**移动端自动播放被拦 → 「▶ 轻触屏幕开始」手势解锁**（不跳过视频）；多重兜底（error / 15s 超时）
- 🪧 **极限模式终点 = 指示牌**（主人设计 11 形状：木柱 + 深棕大牌 + 箭头 + 白色「雪山出口」文字，drawSignData 数据版绘制，0.35 缩放）；通关字幕按模式分派：极限「**恭喜你，走出了雪山！**」/ 普通「恭喜你，你成功到达了雪峰!」
- ☁️ **Netlify「服务暂不可用」修复**：Edge Function **Origin 白名单**加入 `https://nailong-run3.netlify.app`（4 函数 auth/score/equip/achieve 全部重新部署）；⚠️ 踩坑记录：**Supabase CLI 部署源 = `supabase/functions/`**（非权威版 `edge-functions/`），须双目录同步否则白改
- 🔄 **成就同步延迟优化**：连发成就**合并去重**（同一时刻 N 个成就只发 1 次全量，串行不乱序不丢最新）；fetch 加 **15s 超时**（AbortController，弱网不挂起）
- 🔐 **换设备登录修复**：服务器成就拉回后**刷新 UI**（无尽按钮/依神卡/成就面板即时解锁，不再停留旧锁定态）；**头像跨设备同步**（getProfile 返回的 leaderboard.avatar 拉回 → 写本地 nlAvatar_<昵称> + 标记已同步 → 主菜单头像即时更新；此前只有排行榜同步）
- 🎛️ **手机按钮布局自定义**（帮助 → 操作设置 → 🎛️ 按钮布局）：三个按钮（跳/蹲/💨）全部显示可**拖拽**（横竖屏旋转感知坐标，跟手）；**单击选中** → 上方弹出**大小/透明度调节框**（自定义滑块，旋转感知可拖）；**布局时隐藏主菜单**；**↺ 恢复默认**按钮（清 nlBtnLayout + 按钮回默认位）；保存位置/大小/透明度（localStorage.nlBtnLayout）
- 📱 **手机触控**：经典按键模式新增 **💨 技能按钮**；上下分区模式**双击跳区 = 技能**（300ms 内第二击空中喷气）；**toast 挂 #stage 内**（竖屏随画面转正，微信/QQ 不再歪 90°）；竖屏面板定位全家桶修复（布局面板/完成按钮/角色面板放回 #stage 内跟随旋转正立 + 旋转感知局部坐标定位）
- 📢 **播报队列**：游戏内横幅提示（雾/雪/暴风雪/缠身/奶鱼）统一**屏幕中上方堆叠**（最新在下 y58、旧的逐条上移、触顶/超时消失、同文案去重、上限 4 条）；暴风雪提示从 toast 改上方横幅（与其他天气同格式）；**暴风雪限定极限模式**（硬保险：非极限强制关）
- 🌿 **荆棘/细雪 5 层全齐（主人手绘）**：荆棘 L1/L2 新画 + L3/L4/L5 重排（8/14/21/36/58 形状）；细雪 L4/L5 新画（17/18 雪团，负 x 对称件自动镜像防错位）；渲染逻辑去掉全部程序化加厚 fallback（按层直出真实数据）；**奶龙荆棘 L4/L5 分布优化**（角度范围加大 ±1.4、垂直跨度 15→20、起点错开防并排）
- 🎵 **音频优化**：极限 BGM 192k → **128k**（5.55MB→3.7MB，时长 231.233719s 完全一致不影响通关判定，原版备份 .bak）；BGM **Blob 预加载**（fetch 整首 → 本地播，移动端进度条不再卡）
- ⌨️ **电脑端新键位**：跳跃 W/↑；蹲/滑铲 S/A/←/↓（新增 A、←）；技能 空格/→；帮助面板键位说明同步
- 🔋 **开局屁量回满 100%**（reset 重置 fartGauge/fartGaugeShown——上一局剩余不再带进下一局）
- 📖 **游戏介绍完善**（帮助面板）：全机制覆盖——三模式说明 / 电脑手机操作 / 角色 / 昼夜天气 / 障碍物 / 技能系统 / 鱼干护体 / 成就排行 / 账号数据（不含具体数值）
- ✅ **成就上传数据库核实**：users.achievements（jsonb）实测有数据（随枫 8 成就全 true）；装备存 leaderboard.equip（RPC update_equip 白名单 8 key）

---

## v3.1.6（2026-08-11）— 极限模式视觉主题化 + 工坊增强

- 🏔️ **极限模式雪山场景**（drawBackgroundSnow）：三层雪峰群视差滚动（0.144×/0.36×/0.45×）+ 雪坡 + 雪痕（与障碍物同速）；**夜晚统一月光蓝调**（天空 #1E2436、主峰 #5A6A88、雪坡 #37435C、微亮月线）——修复主峰夜晚不压暗的问题；星星渐显
- 🪨 **极限模式石块 = 主人设计**（ROCK_JSON 数据版）：多边体 + 高光 + 裂纹 + 白色雪斑（10 形状，工坊绘制）；**夜晚雪斑渐变月光蓝**（石体/高光/裂纹保持原色）；锚点=地面 + y 翻转
- ❄️ **极限模式荆棘丛 = 细雪雪堆**（BRAMBLE_JSON 数据版）：3 片大雪堆 + 细雪线（13 形状）；生成尺寸**宽 40~70 / 高 30~50 独立随机**、**钟形分布中间大两边小**（sin 曲线）；**夜晚集体变月光蓝**（雪堆 #8CA3C8）；游戏内提示改「❄️ 细雪缠身」
- 🌨️ **细雪缠身三层**（SNOW_THORNS L1/L2/L3 = 5/7/11 雪团，工坊绘制）：**依神**世界坐标直画（1.2 倍等比 + 偏移微调）；**奶龙**依神坐标映射局部（2.8 倍等比 + 右移 3px + 上移 22px + **下蹲随身体压缩 1.15×0.72/跳跃拉伸/前倾**）；修复**浮动不同步** bug（performance.now → dno.animT 同一时间源，暂停后相位一致）
- 🚁 **极限模式直升机 = 主人新设计**（HELI_JSON 24 形状，黄色机身 + 座舱 + **贝塞尔曲线尾饰**——游戏首次支持 path 类型）；**吊挂纸箱**（assets/cargo_box.jpg 主人照片，40.3×46 等比、绳子 59px 机腹→箱顶）；碰撞尺寸同步纸箱比例；资源预加载计数 5→6
- 🎨 **绘制工坊增强**：新增 **✂️ 切割功能**（布尔交集：选中图形 ∩ 参照图形 = 保留重合部分；Sutherland-Hodgman 裁剪，支持矩形/椭圆/三角/多边形，线段不支持；结果转 poly 可撤销）；参照下拉改**中文命名**（#id 类型 尺寸（部位））；图形列表加尺寸显示；heli3 模板更新为主人新设计（24 形状）+ 下拉改名
- 🔄 **模拟窗同步增强**：heli3 卡片（极限版）+ drawShapesAt 支持 **path 贝塞尔**；修复 heli3 卡片空白 bug（对象 vs 数组）；**工坊窗口 focus 自动载入同步标记**（无需刷新）；模拟窗写入不再覆盖工坊存档（保留原数据只追加标记）
- 🐊 成就「鳄啊~」：一局撞击 3 次 → **5 只**；🌵 荆棘行者描述改「缠绕荆棘/**细雪**时连续前进 2500 分」

---

## v3.1.5（2026-08-11）— 无尽模式完整版（开场动画 + 焦土场景 + 循环 BGM + 时间计分）

- 🎬 **开场动画 4.2s**：雪山奔跑 → **巨型山形三角横扫转场**（0.9s，三角形内=焦土、外=雪山，尖端余烬微光，lerpColor clamp 修复）→ 焦黑废墟 + 字幕「♾️ 无尽模式 · 雪山之后，是无尽焦土」（停留 1.0s）；动画期间 `acc` 清零（障碍不瞬涌）
- 🏚️ **游戏场景 = 焦土废墟**（drawBackgroundRuin + drawRuinElements）：远景剪影 9 座 / 断壁 5 面 / 拱门 3 座 / 电线杆 / 栅栏 / 枯树 / 焦痕 / 裂纹 / 砖瓦 + 灰烬粒子 46 颗；动画末滚动量续接（RUIN_SCROLL_OFFSET=325）无缝循环；**修复 JS 负模 bug**（跑久后全部元素消失——取模改正模写法）；**图层梳理**（建筑低于地平线、贴地纹理最上层、砖瓦压线）；地平线浅棕 #8E706A
- 🎵 **BGM = 灾难土壤**（assets/bgm_endless.mp3）**交叉重叠循环**：A/B 双实例，主轨剩 3s 提前播另一轨（两遍重合 3s），无缝无限
- ⏱️ **得分按存活时间**：endlessTime 计时 × 50/秒（跑得快不加分，活得久才是王道）；进度条隐藏（无终点）；终点雪山/镜头拉远禁用；ending 恒 false（每遍末尾不禁障碍/天气）
- 🏃 场景速度分层：**地平线以下贴地纹理与障碍物同速**（scroll=d_sco×50）、地平线以上建筑恢复原视差速度（换算系数 0.144~0.45）
- ✍️ 「随枫出品」水印跟随场景明暗（hudCol 渐变）；所有 toast 提示统一 74% 高度
- 🏠 **结算界面「主菜单」按钮**：与「🦖 再来一局」并排（endBtns 按钮组：橙/蓝双色、横屏跟随画布定位）——死亡/通关后可直接回主菜单（刷新无尽锁定态 + 排行榜 + 成就同步）

---

## v3.1.4（2026-08-10）— 角色界面重构 + 依神解锁 + 极限模式鱼干规则

- 🎭 **角色界面重构**：点击 🎭 弹出面板改为**贴游戏画面右侧**（JS 按 canvas 实际范围定位，右缘/下缘与画面完全重合，不再超出窗口）；布局分**两列**（👗 服饰 + 🎭 角色）带渐变**分割线**；服饰列占位选项移除（留空待填充）；角色卡上下排布
- 🔒 **依神解锁系统**：需**普通模式通关**（summit 成就 = 到达终点）解锁；未解锁 = 卡片置灰 + 🔒 + 点击拦截提示「通关普通模式才能解锁依神！」；多层防御（点击校验 / setChar 内校验 / localStorage 已选但未解锁自动回退奶龙 / 打开面板实时刷新）
- 🐟 **极限模式鱼干规则**（替代临时无限护体测试）：**不生成奶鱼**（中途无补给），**初始鱼干数 = 已解锁成就数**（成就积累 = 开局资源），用尽一撞即死；普通模式保持 10 条 + 正常生成
- 🔋 **屁量条升级**：**平滑消耗**（实际值即时扣减判定、显示值指数缓动滑落，不再瞬间跳变）；恢复速率 8%/秒 → **5%/秒**（20s 回满）；**≥40% 可释放时外圈蓝光呼吸脉动**（0.6~1.0 强度、约 1.9s 周期）；「屁量不足」提示下移至 74%
- 🐛 **头像按账号隔离**：存储键 `nlAvatar` → **`nlAvatar_<昵称>`**（含同步标记键）——修复注销旧账号后新注册账号仍显示旧头像、且自动补传把旧头像上传给新账号的问题（4 处引用：登录显示/自动补传/上传存储/成绩提交携带）
- ☁️ 确认解锁数据上云：依神（summit）/无限模式（extremeWin）判定走成就系统，`saveAch → syncAchievements → Edge Function achieve` 按账号存储，登录时 `getProfile` 拉回合并（跨设备一致）

---

## v3.1.3（2026-08-08）— 依神动作精调 + 放屁系统升级

- 🧎 **依神「蹲」动作**（主人自定义）：缠荆棘时按 ↓ = 深蹲（头 -0.79 / 大腿 +1.8·+2.31 / 脚 -2.76·-2.13 / **后仰 35°**），无荆棘按 ↓ = 滑铲不变；蹲贴地补偿重新实测（SQUAT_LIFT 49.9）+ 碰撞框 52×52
- 🦘 **依神「跳跃」动作**（主人自定义）：头 0.09 / 手 -0.32·-0.17 / 腿 +0.63·+0.57 / 脚 -1.37·-0.96 / **后仰 12°**，替换原通用收腿动画
- 💨 **依神「喷气」动作**（主人自定义）：放屁时姿态（头 -0.29 / 手 -0.9·-1.41 / 腿 +0.54·-0.69 / 脚 -2.04·-0.57 / **前倾 12°**），放屁结束自动回原姿态
- 🔄 **精细姿态过渡系统**（主人定稿）：进入滑铲/蹲 = 头+双手+躯干平滑 0.12s、**腿脚瞬切**；跑→滑铲躯干也瞬切（秒趴）；**滑铲/蹲→跑 = 全部整体平滑**（含位置 lift 插值，修复此前「回跑步位置瞬移 27.6px」bug——fromDuck 标记持久化）
- 📐 **依神整体下移 3px**（YISHEN_FOOT_OFFSET）：渲染/碰撞箱/荆棘三层同步，消除「感觉腾空」
- 💨 **放屁冲屏位移移除**：不再前后猛移画面（fartDx 恒 0，保留恢复注释）
- 💨 **屁云长条喷气轨迹**：连续生成 + 高速左移 30~45px/s + 寿命 0.4~0.6s；起点按角色区分（奶龙 = 左侧偏右上 / 依神 = 臀部区域 +46,-44）
- ⏱️ **放屁时间轴**：冲刺 0.5s→**0.3s** + 悬浮 0.2s→**0.3s**（推力 ×3→×1 线性衰减同步压缩）；悬浮段速度 **×0.5**（缓慢滑行）
- 🔋 **屁量系统**（新）：右上角→**左侧中间蓝色竖条**（渐变 + 发光 + 高光，10×150px）+ **40% 阈值刻度线**；随时间恢复 **8%/秒**；**每次放屁耗 40%**、≥40% 才可释放（不足 toast 提示）
- ⚙️ **设置（暂停）按钮**：调大（1em→**1.25em**）+ 右缘内移（0.5em→1em）
- ⚡ **快落限定跳跃后半段**：`vy ≥ 0`（过顶点）按 ↓ 才触发重力 ×4，上升段无效

---

## v3.1.2（2026-08-08）— 角色切换 + 依神实装（主角双角色）

- 🎭 **主菜单「角色」入口**（原占位实装）：点击隐藏主菜单 → 进入「🎭 角色与服饰」页面（仿成就面板）；角色卡片 🦖 奶龙（默认）/ 🧙 依神，点击即切换 + 金色高亮；👗 服饰区占位（开发中）；选择存 `localStorage['nlCharSel']`，刷新记忆
- 🧙 **依神游戏实装**（v3.2 核心）：新模块 `dev-tools/yishen-render.js`（连接点树 8 部位渲染 + 角度表，游戏专用版）；`drawChar()` 分派（依神 → drawYishen / 奶龙 → drawDino）；数据 `yishen-data.js`（桌面微调版 `依神 (1).json`，bbox 89×233，图层按备注重排 `s7→s6→s8→s3→s5→s2→s1→s4`）
- 🦆 **蹲 = 滑铲**（主人自定义动作存档 `yishen-poses.js` 实装）：lean -73° 大幅前倾俯冲 + 双腿前伸 + 左腿深屈膝 -2.23；碰撞自动切**低矮框**（高 41px，钻障碍用）
- 🦘 **跳跃 = 主人自定义「跳跃」动作**：头 +0.09 / 手 -0.32·-0.17 / 大腿 +0.63·+0.57 / 脚深屈 -1.37·-0.96 / 身体后仰 12°（蜷身腾空姿态）
- 📏 **碰撞箱适配**（实测校准）：`bboxOfRot` 旋转感知包围盒（原 bboxOf 不含 rot 致陷地 4.8px）；脚底贴地补偿（站姿 -1.2 / 滑铲 -0.2）；**中心对齐渲染中心**（依神数据世界中心 x≈108，原碰撞箱偏左 45px）；滑铲最低点比站姿高 57.6 世界单位单独补偿；`YISHEN_SCALE=0.48`（主人要求放大，碰撞/荆棘自动跟随）
- 🌿 **依神荆棘三层（主人手绘）**：`dev-tools/yishen-thorns.js`（`window.YISHEN_THORNS[1/2/3]`，数据版优先，缺省代码版 fallback）；游戏侧 `drawOneShapeAt` 扩展 **tri/line** 类型（工坊同规则：等腰/直角三角 + 描边折线）；荆棘在世界坐标变换内绘制（跟随姿态/前倾/滑铲）；**绿叶位移修复**：主人装饰原「关于画布 x=0 对称」（左侧镜像跑到角色左侧 200 世界单位）→ 19 个形状镜像回身体中心（x=100.26）另一侧，实测范围 [74,127]
- 🦖 **奶龙腿部动画迭代**（主人逐条纠正定稿）：长椭圆 **13×8.8**（原 8px 近圆「只能看到一小点」）；旋转摆动（髋 -8，±0.5 rad）+ **反相平移 ±4** + 上下浮动 ±5（分开时上抬/回中贴地）；右腿深色 **#F5C84A** 且**底层先画**（左腿 #FFD93D 上层）；无脚掌、无前倾
- ✂️ **绘制工坊 Ctrl+C / Ctrl+V / Ctrl+X**（形状复制/粘贴/剪切）：多选复制、粘贴 +12 偏移新 id、连接点 link 自动断开、支持撤销
- 🌿 **工坊荆棘模板**：TEMPLATES 新增 `thornYishen1/2/3`（🌿 荆棘·依神L1/L2/L3，含灰色躯干参考可删），下拉即载入——主人手绘后导出回流游戏
- 🧙 **模拟窗同步**：引入手绘荆棘（依神预览 + 🌿 1/2/3 层直接缠身，不再切回奶龙）；drawOneShapeAt 扩展 tri/line；跳跃动作与游戏一致

---

## 规划：v3.1 — 无尽模式

- ♾️ 通关极限模式（extremeWin 成就）解锁，无终点 BGM 循环、分数突破 9999、速度无上限递增

---

## v3.1.1（2026-08-07）— 加载界面草坪化 + 草坪演示动画

- 🌤️ **加载界面改造**：深色背景 → **草坪场景**（天空渐变 / 视差云 / 远山 / 绿地 / 滚动小草），奶龙奔跑 + 金色进度条保留；标题加深色描边；**主人选择黑夜版**（星空闪烁 + 暗色远山/草地，PAL.night）
- 🖥️ **ESC 暂停（电脑端）**：游戏中按 ESC 暂停（面板弹出 + BGM 冻结 + _st_t 补偿），再按 ESC 继续；与切后台自动暂停共用 pauseGame/resumeGame 封装
- 🦖 **独立演示动画页** `demo-grass.html`：纯草坪场景 + 奶龙奔跑（无 UI / 无障碍 / 无 BGM），资源 100% 复用游戏绘制与白天调色板，16:9 自适应
- ♾️ **无尽模式演示动画页** `demo-endless.html`：**雪山 → 巨型三角转场 → 焦黑废墟**（游戏开场动画 1:1 复刻）+ 废墟奔跑 15s 循环 + **灾难土壤 BGM 循环**（点击开始启动，浏览器自动播放限制）；含飘雪/灰烬粒子与「♾️ 无尽模式」字幕；**主角 = 依神**（复用 yishen-data/render 模块，连接点树姿态与游戏实装一致）
- ✅ 实测：天空/绿地/进度条/奶龙像素确认 + 动画动态

---

## v3.1.0（2026-08-07）— UI 打磨与细节修复

- 🏷️ 游戏内全部标题 **「奶龙快跑」→「奶龙快跑3」**（浏览器标签 / 开始界面 / 主菜单 / 加载封面 / PWA manifest）
- 🦖 去掉浏览器标签 + 加载封面文字的恐龙图标（favicon 保留）
- 🏆 排行榜表头「昵称」→「**玩家**」；行内头像微调左移（margin-left -0.1em）
- 🎨 **帮助按钮格式统一**（字号 0.71em / padding 0.2em 0.7em / 深色底 #2A2A3E，与成就一致）；帮助+成就整体下移右移贴右下角；🚪退出贴左下角（left -1.8em / bottom -0.2em）
- 🎭 主菜单新增「**角色**」按钮占位（帮助与成就之间，v3.2 实装角色切换系统）
- 🗑️ 游戏画面**最高得分显示删除**（HUD / 撞到结算 / 通关结算 3 处；best 记录逻辑保留供成就系统）
- 🐟 奶鱼计数 HUD **右上移**
- 🌱 草坪小草滚动方向修复（`groundOffset` 反向 → 环境后退方向一致）+ **JS 负数取模坑**：`(x-Δ)%24` 负值不折返会无限负增长 → 需 `((v%m)+m)%m` 正模
- 💨 放屁冲刺时长 **0.8s → 0.5s**（主人试手感定稿）；冲屏曲线**线性回归**（曾试 60ms 极陡 30px 版，主人要求回线性 0→46px）

---

## v3.0.9（2026-08-06）— 新技能：放屁（冲刺+悬停两阶段）

- 💨 **放屁技能（空格触发）**：**按下略微向上**（离地 10px）→ **向前快速平移 0.5s 由快到慢**（推力衰减 ×3→×1 线性，喷气刚放最猛）→ **空中停留 0.2s**（原速悬浮）→ **恢复**（原速 + 重力落地）
- 🎬 **冲屏动效**：冲刺前半段角色**猛向右移**（绘制偏移 0→46px），后半段**抛物线缓慢归位**（尾段最慢）——纯视觉偏移，物理碰撞不动
- ⚙️ 两阶段状态机：`fartTimer 0.5s` 冲刺（屁云喷气 + 速度衰减 + 冲屏）→ `fartHover 0.2s` 停留 → 结束恢复；全程 `vy=0` 悬浮不受重力
- 🎮 **操作**：键盘 **空格** = 放屁（原空格跳跃改到 ↑/W）；触屏右半仍为跳跃
- ⚡ **空中触发**：冻结垂直轨迹，只加冲刺+悬浮（防叠加）；快落（按蹲）优先可急停
- 🔊 **音效**：WebAudio 合成屁声（160→45Hz 方波下滑 0.38s）
- ✅ 实测：阶段1 spd 6.0→15.1 + 离地 10px；阶段2 spd 6.1 悬浮 0.2s；结束落地
- 🐛 修复①：初版 vy=-10 与跳跃同高（「跳高了」）→ 离地 10px 悬浮
- 🐛 修复②：**空格+↑同按飞升** → `fartActive` 期间禁跳 + 垂直轨迹冻结；实测同按高度=普通跳
- 🐛 修复③：**全局一直加速**（冲刺写进累计距离 d_sco）→ 改 `spd` 临时乘数，结束即恢复
- 🔄 技能迭代：冲刺加速 ×2.5/0.6s → 喷气悬停减速 ×0.3/0.2s → 冲刺 0.8s+悬停 0.2s → **冲刺 0.5s + 悬停 0.2s（主人定稿）**

---

## v3.0.8（2026-08-06）— 动作模组：空中快落（fast-fall）

- ⚡ **新增快落**：跳起滞空时按住「蹲下」（触屏左半/键盘↓S/经典蹲键）→ **重力 ×4 快速下落到地面**
- 🦆 落地时仍按着蹲 → 直接进入**蹲姿**（松开恢复站姿）；松开蹲键可随时取消快落回到普通下落
- 🔧 实现：新增 `duckHeld` 按住标记，重力分支按 `duckHeld && mode==='jump'` 切换 0.55→2.2（×4，可调）；三操作渠道（键盘/触屏分区/经典按键）自动生效；雪地空中减速不影响（重力独立）
- ✅ 实测：跳跃顶点按蹲 → 300ms 内从 84px 高空砸地 + 蹲姿

---

## v3.0.7（2026-08-06）— 排行榜头像展示 + 头像云端同步

- 🏆 排行榜每行**昵称与成就图标之间加头像展示**（1.25em 小圆头像，默认头像兜底，结构：`[成就图标][头像][昵称]`）
- ☁️ **头像上云**：`leaderboard` 表加 `avatar` 列（公开只读随查榜一次带出，零额外请求）
- 🔄 **同步链路**：上传头像 → 本地 + `auth setAvatar`（update-only，不碰 score）；**未上榜玩家**头像随提交分数（`score` 函数 + RPC 新参 `p_avatar`，`coalesce` 保留旧头像）一起落库
- 🔒 校验：setAvatar/score 的 avatar 必须 dataURL（jpeg/png/webp）且 ≤40KB；token 归属校验不变
- ⚠️ 坑：avatar 曾用 `upsert` 整行 → score NOT NULL 报 400（幸好未清分数）→ 改 update-only + 分数时补头像

---

## v3.0.6（2026-08-06）— 主菜单登录者信息（头像 + 昵称）

- 👤 主菜单**底部栏（左下角）登录者信息**：圆形头像框 + 昵称**横排**（头像左、昵称右），整体缩小；底部栏统一容纳 头像昵称 + 🚪退出 + 📖帮助（flex 布局，昵称长不遮按钮）
- 🖼️ **头像上传**：点击头像框 → 选择图片 → **中心裁剪正方形** → 压缩 114×114 JPEG → localStorage 保存（`nlAvatar`），默认头像 `assets/avatar_default.png`
- 🔄 进主菜单自动刷新（昵称随登录账号变化）；上传失败/超限有 toast 提示
- ⚠️ 当前头像**本地存储**（不跨设备）；后续可同步 Supabase（users 加 avatar 字段）
- ✏️ 昵称字数限制 **12 → 10**（前端正则/输入框/提示 + 4 个 Edge Function + 3 个 RPC 全链路同步，实测 11 字拒 / 10 字过）

---

## v3.0.5（2026-08-06）— 无尽模式入口（占位）

- 🎛️ 主菜单模式选择新增 **♾️ 无尽模式**按钮（紫色 #7C4DFF，普通/极限下方）；布局微调（标题-按钮间距压缩 + 左列整体上移）
- 🔒 **解锁检测**：点击校验 `extremeWin` 成就（通关极限模式）——未解锁提示「通关极限模式才能解锁无尽模式！」；已解锁提示「开发中敬请期待」（玩法 v3.1）
- 🎨 **锁定态视觉**：未解锁按钮灰色 #666，解锁后紫色 #7C4DFF（进主菜单/回主菜单自动刷新）
- 💬 新增轻量 `showToast` 提示组件（中央偏下浮层，1.8s 自动消失）

---

## v3.0.4（2026-08-06）— 极限模式专属 BGM

- 🎵 **双 BGM 架构**：普通模式 203s（assets/bgm.mp3）/ 极限模式 **231.2s**（assets/bgm_extreme.mp3 = HOYO-MiX 官方「炽烈的还魂诗·再现」，128k 立体声 3.7MB）
- 🔀 新增 `curBgm` 按模式切换（startGame 时设置），全部 BGM 引用点（计分/进度条/ending/镜头拉远/暂停/死亡/兜底补播）改为当前模式对象
- ⏱️ **游戏时长联动**：极限模式通关时长 = 231.2s；进度条按百分比自动适配（终点 🏔️ 不动）；ending（剩余≤6s）、镜头拉远（剩余≤5s）、雷暴过半触发全部按新模式 BGM 判定
- 📏 **降级总距离**：新增 `DIST_EXTREME=2846`（231.2s × 速度公式推导 D(231.234)=2845.9），普通模式保留 2330；BGM 故障时进度降级按模式总距离
- 🛡️ **反作弊 RPC 同步**：`upsert_leaderboard` 偏差基准 203 → **231**（极限模式 231.2s 取整），实测：9999+231s 通过 / 外挂短时长全拒
- 🔊 **音频处理（定稿）**：官方 128k 源一次处理（+3dB + 轻 limiter，避免二次编码损失），输出 **192k 立体声 5.5MB**，mean **-10.9dB**（比普通响亮 1.3dB）；⚠️ 教训：128k 源再压 128k + 重压缩 limiter 会糊（主人反馈后重做）；极致音质需无损源
- 🐛 **修复切后台挂机 bug**：浏览器切后台/切标签页时 rAF 被节流（画面冻结）但 BGM 音频继续走（分数=BGM 进度→继续记分/可能假通关）——新增 `visibilitychange` 监听自动暂停（冻结 BGM + 显示暂停面板）
- 🐛 **修复暂停时间污染反作弊时长**：submitScore 用 `Date.now()-_st_t`，手动暂停/后台暂停的时间原会计入 elapsed 导致偏差校验误拒——新增 `pauseGame/resumeGame` 封装，恢复时 `_st_t` 补偿暂停时长（elapsed = 纯游玩时间）
- ⚙️ 预加载 TOTAL_RES 4 → 5（新增极限 BGM）

---

## v3.0.3（2026-08-06）— 安全体检 + 网络请求校验

- 🔍 **接口保护体检**（抓包者视角全表扫描 REST/RPC/函数三层）：
  - 发现并修复 **RPC PUBLIC 隐式授权漏洞**：`revoke execute from anon` 无效（函数默认授权 PUBLIC，proacl 中 `=X/postgres`）→ 补 `revoke execute from public`，修复后匿名调 RPC 全 401
  - 确认读 users/score_log 返回 200 空数组 = RLS deny 标准行为（无数据泄露，非漏洞）
- 🛡️ **Origin 白名单**（4 个 Edge Function 入口统一校验）：
  - 无 Origin（curl/python 裸调）→ 403 拒绝
  - 陌生站点 Origin → 403 拒绝
  - 放行：`https://kazuha233.github.io`、`null`（file:// 本地预览）、`http://localhost*`
- 🧪 **参数类型校验**（防畸形请求，校验先于 token 查询）：
  - score：必须数字 0~9999（字符串/负数/超限 → 400）
  - elapsed：必须数字 ≥0
  - equip：8 种成就 key 白名单前置校验
  - achievements：8 key 白名单 + 值必须 boolean
- 🐛 前端 `submitScore` 补本地校验（分数/时长异常值不提交）
- 🧹 测试账号全清理，榜单归零

## v3.0.2（2026-08-06）— Edge Function 部署 + 部署坑修复

- 🚀 **4 个 Edge Function 部署上线**：
  - `auth`：注册 / 登录 / 改密码 / getProfile（bcrypt 哈希、旧 SHA-256 懒迁移、5 次失败锁 15 分钟防爆破、改密码吊销旧 token）
  - `score`：token 归属校验 → 调库内反作弊 RPC（限流/偏差≤20%/超速约束）
  - `equip`：token 归属校验 → 装备白名单 RPC
  - `achieve`：token 归属校验 → 成就白名单 RPC
- ⚙️ **部署坑修复**：
  - Edge Functions 默认 `verify_jwt=true`（裸请求 401 `UNAUTHORIZED_NO_AUTH_HEADER`）→ `--no-verify-jwt` 重部署，安全全交给函数内 token 校验
  - 注册 400：`users.password` NOT NULL 与新架构冲突 → `alter column password drop not null`
- 🔑 supabase CLI 2.111.0 安装（npm 官方源；npmmirror 镜像缺 win32 二进制包）

## v3.0.1（2026-08-06）— 方案 C：Edge Function 全代理（前端零写 key）

- 🔐 **前端写接口零 key**：注册/登录/提交分数/佩戴/成就全部改走 `/functions/v1/*`（token 放 body），删除全部 `apikey`/`Authorization` 写请求头——抓包只剩空壳
- 🏆 **排行榜查询保留 REST 公开只读**（`read_any` 策略，只读不可写；不走 Edge Function 省额度）
- 🗄️ **数据库加固**：
  - users 表加 `token`（会话令牌）/ `pass_hash`（bcrypt）/ `fail_count` / `last_fail`
  - 删 users 表 read/insert 策略（防拖哈希/直插）、删 score_log 全部策略、删 leaderboard insert 策略（防伪造成绩）
  - 3 个 RPC（upsert_leaderboard/update_equip/sync_achievements）仅 `service_role` 可调
- 🔑 登录态改造：`nlUser` 存 `{nick, token}`（旧 `{nick, hash}` 自动判未登录需重登）；新增 `handleAuthFail` 401 统一踢回登录界面（防抖）
- 🧩 新增 `fnFetch` 封装（写接口统一入口）+ `FUNCTIONS_URL` 常量
